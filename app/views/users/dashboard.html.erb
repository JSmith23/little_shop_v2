<main class='dashboard'>
  <h1><%= @greeting %></h1>
  <%= link_to  "My Orders", dashboard_orders_path if @orders.length > 0 %>
  <%= link_to  "My Items", dashboard_items_path if @items.length > 0 %>
</main>
