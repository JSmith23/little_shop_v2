<main class='users-index'>
  <h1>All Users</h1>
  <% @users.each do |user| %>
    <h4><%= link_to user.name, user_path(user) %></h4>
    <h5>Status: <%= user.enabled ? "Enabled" : "Disabled" %></h5>
    <% disable_button_text = user.enabled ? "Disable" : "Enable" %>
    <%= button_to disable_button_text, user, :method => 'delete' %>
  <% end %>
</main>